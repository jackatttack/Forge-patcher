# Forge-patcher
Minimal Forge is a clipboard-based patching harness for Pythonista. AI writes plain-text bundles, you run forge_entry.py, and Forge returns a run packet for inspect → patch → test loops on-device. Core ops only; custom workflows can be added.
