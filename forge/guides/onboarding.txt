# onboarding

## SUMMARY
Start here if you are new to Forge. Forge lets an LLM help you work on local code while you stay in control.

## WHEN TO USE
Use this when opening Forge for the first time, coming back after a break, or helping someone understand the basic workflow.

## WHAT FORGE IS
Forge is a local Pythonista development harness.

The LLM writes a plain-text bundle. You choose when to run it. Forge runs it locally and returns a run packet showing exactly what happened.

The packet is the source of truth.

## FASTEST START
From LinkOS Home, tap Start.

Start copies the first-time LLM guide to your clipboard. Paste that guide into an LLM before running any Forge bundle.

The guide teaches the LLM what Forge is, what a runnable bundle looks like, and exactly what first bundle it should give you.

## MANUAL START
If Start cannot copy automatically, manually copy the contents of:

    docs/AI_FIRST_BOOT.txt

Paste that text into an LLM. Do not run it as a Forge bundle.

## THE BASIC LOOP
1. The LLM gives you a Forge bundle.
2. You copy the bundle.
3. You run the Forge Shortcut.
4. Forge executes the bundle on your device.
5. Forge copies a run packet back to your clipboard.
6. You paste the packet back into the chat.
7. The LLM reads the packet and decides the next safe step.

Nothing is confirmed until the packet comes back.

## PROJECT SCOPE
Forge runs against an active project root.

In contained mode, Forge works inside its own install folder. This is safest for first use.

In root launcher mode, Forge launchers live at Pythonista Documents root and can work across your wider Pythonista workspace.

Use [[root-launcher-mode]] when you want more power.
- [[concepts]] — key vocabulary
- [[the-loop]] — how the clipboard workflow works
- [[run-packet]] — how to read Forge output
- [[root-launcher-mode]] — contained mode versus wider workspace access
- [[tutorial]] — learning hub
- [[tutorial-loop]] — first hands-on lesson

## FIRST THINGS TO LEARN
Start with:

- [[concepts]] — key vocabulary
- [[the-loop]] — how the clipboard workflow works
- [[run-packet]] — how to read Forge output
- [[tutorial]] — learning hub
- [[tutorial-loop]] — first hands-on lesson
- llm-first-time-guide — teaching contract for an LLM
- concepts — vocabulary
- the-loop — clipboard workflow
- run-packet — reading packets
- root-launcher-mode — project scope and wider access
- tutorial — learning hub
- safe-patch — safe edits and recovery
    patch small
    verify with the packet

## RELATED
- llm-first-time-guide — teaching contract for an LLM
- concepts — vocabulary
- the-loop — clipboard workflow
- run-packet — reading packets
- tutorial — learning hub
- safe-patch — safe edits and recovery
