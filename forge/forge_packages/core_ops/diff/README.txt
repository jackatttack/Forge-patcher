DIFF reads run snapshots and compares file changes.

It is a recovery and inspection op, not a mutating op.

Use DIFF current inside a running bundle to inspect changes already made earlier in the same bundle.

Use DIFF latest or DIFF <stamp> to inspect a stored run.
