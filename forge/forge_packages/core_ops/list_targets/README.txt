LIST_TARGETS is the reboot AST discovery op.

It lists functions, classes, methods, and simple assignments using the same target syntax that AST preview and future AST write ops use.

This should be used before REPLACE or INSERT-style AST mutation.
