list_ops
========

LIST_OPS shows the reboot op registry.

It proves that package-shaped ops are discoverable without central wiring.

Purpose:

    package-shaped folder -> registry discovery -> visible command vocabulary
